/**
 * Represents the payload of a network message, which can be either a string, an ArrayBufferView, or an ArrayBuffer.
 *
 * @export
 * @type {NetworkPayload}
 */
export type NetworkPayload = string | ArrayBufferView | ArrayBuffer;